from UPAPEDRO import DADOS DO PACIENTE
def principal():
    
    dados_atendimento = []
    while True:
        entrada = str(input("Deseja inserir dados: (Sim/Sair); "))
        if entrada == 'Sair':
            break
principal()

